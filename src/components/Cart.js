import React, { Fragment } from "react";
import Title from "./Title";

class Cart extends React.Component {
  render() {
    return (
      <Fragment>
        <Title text={"Cart"} />>
      </Fragment>
    );
  }
}

export default Cart;
